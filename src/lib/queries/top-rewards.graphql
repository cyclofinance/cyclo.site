query TopRewards {
  trackingPeriods (where: { period: FEB_1 }) {
    period
    totalApprovedTransfersIn
  }
  trackingPeriodForAccounts (where: {period: FEB_1}, orderBy: netApprovedTransfersIn, orderDirection:desc) {
    account {
      id
    }
    netApprovedTransfersIn
  }
}