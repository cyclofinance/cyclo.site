query AccountStatus($account: String!) {
  trackingPeriods(where: { period: FEB_1 }) {
    period
    totalApprovedTransfersIn
  }
  trackingPeriodForAccounts(where: { account: $account, period: FEB_1 }) {
    netApprovedTransfersIn
    period
  }
  sentTransfers: transfers(
    where: { period: FEB_1, from: $account }
    orderBy: blockTimestamp
    orderDirection: desc
    first: 1000 
  ) {
    fromIsApprovedSource
    blockTimestamp
    transactionHash
    from {
      id
    }
    to {
      id
    }
    value
  }
  receivedTransfers: transfers(
    where: { period: FEB_1, to: $account }
    orderBy: blockTimestamp
    orderDirection: desc
    first: 1000
  ) {
    fromIsApprovedSource
    blockTimestamp
    transactionHash
    from {
      id
    }
    to {
      id
    }
    value
  }
}