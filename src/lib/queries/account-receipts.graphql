query AccountReceipts($account: ID!, $first: Int!, $skip: Int!) {
	account(id: $account) {
		address
		receiptBalances(
			first: $first
			skip: $skip
			orderBy: balance
			orderDirection: desc
			where: { balance_gt: "0" }
		) {
			balance
			tokenId
			id
			receiptAddress
		}
	}
}
