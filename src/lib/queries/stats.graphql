query Stats {
	eligibleTotals(id: "SINGLETON") {
		id
		totalEligibleSum
	}
	accounts(where: { totalCyBalance_gt: "0" }, first: 1000) {
		id
		vaultBalances {
			vault {
				address
				totalEligible
			}
		}
	}
}
